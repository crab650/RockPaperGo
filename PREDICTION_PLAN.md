# 猜拳 GO｜預測功能開發規格

目前系統版本：`1.2.0`  
文件狀態：規劃中  
目標：利用匿名歷史對局，預測玩家下一次會出石頭、布或剪刀，並用可重現的方式評估模型是否優於基準。

## 1. 第一版目標

第一版先做離線分析與預測評估，不直接影響玩家對戰：

1. 從 `game_rounds` 產生可訓練的序列資料。
2. 建立隨機、出拳頻率與 Markov chain 基準模型。
3. 預測每位玩家下一拳為 `rock`、`paper` 或 `scissors`。
4. 保存模型版本、預測機率、預測結果與實際結果。
5. 在 `/analytics/` 顯示模型準確率及其相對基準的改善幅度。
6. 資料不足時不做個人化預測，改用全體模型。

第一版不包含：讓電腦公開與玩家對打、即時自動重新訓練、深度學習、跨裝置辨識真實身分。

## 2. 可使用的現有資料

- 匿名玩家 ID
- 遊戲場次 ID
- 房間與局數
- 雙方出拳及勝負
- 上一局結果
- 先出拳者
- 玩家累積局次
- 伺服器端與瀏覽器端反應時間
- 語言
- 完成或中止狀態
- 同意、資料格式與程式版本
- 對局及出拳時間

暱稱與 IP 不進入模型。

## 3. 預測單位

每一筆訓練樣本代表「某位玩家即將進行的一次出拳」。雙人完成一局可轉換成兩筆玩家樣本。

預測目標：

```text
target_move ∈ {rock, paper, scissors}
```

建議特徵：

- 玩家前 1、2、3、5 次出拳
- 對手前 1、2、3 次出拳
- 玩家上一局結果：勝、負、平手
- 玩家是否連續使用同一拳
- 玩家三種拳的歷史比例
- 最近 5／10 局的出拳比例
- 房間局數與玩家累積局次
- 上一局先後手
- 反應時間區間
- 語言及程式版本

不得使用當局對手的出拳或任何預測當下尚不可知的資料，以避免資料洩漏。

## 4. 模型階段

### M0：隨機基準

- 三種拳各 `33.33%`。
- 理論準確率約 `33.33%`。
- 用來確認評估流程沒有錯誤。

### M1：全體頻率

- 使用訓練資料的整體出拳比例。
- 永遠預測最常出現的拳。
- 可再依上一局勝負或上一拳分組。

### M2：個人頻率

- 玩家累積至少 10 局才啟用。
- 使用該玩家全部或最近 N 局的出拳比例。
- 樣本不足時退回 M1。

### M3：Markov chain

- 一階模型：依玩家上一拳預測下一拳。
- 二階模型：依玩家前兩拳預測下一拳。
- 可加入上一局勝負與對手上一拳作為狀態。
- 狀態樣本不足時逐級退回一階或全體模型。

### M4：機器學習模型

- 優先考慮 Multinomial Logistic Regression。
- 後續比較 Random Forest 或 Gradient Boosting。
- 僅在 M0～M3 的資料流程與評估穩定後加入。

深度學習暫不採用；目前問題規模小，較簡單的模型更容易解釋與驗證。

## 5. 訓練與測試切分

不可隨機打散所有對局，否則未來資料可能洩漏到過去。

第一版採時間切分：

- 最早 70%：訓練集
- 接著 15%：驗證集
- 最新 15%：測試集

同一遊戲場次必須完整位於同一資料集。分析個人模型時，也必須保證只使用預測時間之前的玩家歷史。

資料量未達 500 局時只顯示描述統計；達 500 局後可試跑基準模型，達 5,000 局後再判斷預測結果是否具有意義。

## 6. 評估指標

主要指標：

- Accuracy：正確預測下一拳的比例
- Log loss：三種拳預測機率的品質
- Macro F1：避免只偏向最常見的拳
- Coverage：系統願意產生預測的樣本比例

商業／遊戲指標：

- 若電腦使用克制預測拳的選擇，其模擬勝率
- 相較隨機基準增加的準確率與勝率
- 不同玩家歷史長度下的表現
- 全體模型與個人模型的差異

第一版成功標準：

- 測試集 Accuracy 穩定高於隨機基準。
- 至少比 M0 高 2 個百分點，且不是由少數玩家造成。
- 訓練流程可重現，同一資料與設定得到相同結果。
- 儀表板清楚標示樣本數與模型版本。

## 7. 新增資料表

### `prediction_runs`

每次訓練或評估保存一筆：

- `id`
- `model_name`
- `model_version`
- `trained_at`
- `data_from`、`data_to`
- `training_samples`
- `validation_samples`
- `test_samples`
- `parameters_json`
- `metrics_json`
- `app_version`
- `schema_version`

### `move_predictions`

每次預測保存一筆：

- `id`
- `run_id`
- `anon_player_id`
- `game_session_id`
- `room_code`
- `round_no`
- `predicted_at`
- `predicted_move`
- `probability_rock`
- `probability_paper`
- `probability_scissors`
- `actual_move`
- `is_correct`
- `history_size`
- `fallback_model`

匿名玩家 ID 可以用於分析，但輸出的公開報表不應顯示完整 ID。

## 8. 指令與模組規劃

建議新增：

```text
rockpapergo/
  prediction/
    __init__.py
    dataset.py
    models.py
    evaluate.py
    service.py
```

CLI 指令：

```powershell
flask --app wsgi train-predictor --model markov
flask --app wsgi evaluate-predictor --run-id 1
flask --app wsgi export-predictions --output predictions.csv
```

訓練應由人工或排程觸發，不應在網頁請求內執行。

## 9. 儀表板功能

在 `/analytics/` 新增「預測模型」區塊：

- 目前模型名稱與版本
- 最近訓練時間
- 訓練、驗證與測試樣本數
- Accuracy、Log loss、Macro F1、Coverage
- 隨機／頻率／Markov 模型比較
- 不同歷史局數的準確率
- 三種實際出拳的混淆矩陣
- 最近幾次訓練的指標變化

所有結果必須同時顯示樣本數，避免在少量資料下誤判。

## 10. 即時預測的第二階段

離線評估通過後，再加入即時預測：

1. 玩家進入新一局。
2. 系統只讀取該時間點以前的歷史。
3. 模型輸出三種拳的機率。
4. 預測結果先寫入 `move_predictions`。
5. 玩家出拳後補寫 `actual_move` 與 `is_correct`。
6. 儀表板比較線上預測與離線測試表現。

若未來加入電腦對手，必須先保存預測，再決定電腦出拳，且不得在玩家出拳後才建立預測。

## 11. 公平性與資料品質

- 排除測試資料、異常機器流量及不合理反應時間。
- 中止局不作為出拳目標，但可分析流失。
- 不宣稱匿名 ID 等於真實獨立人數。
- 不嘗試推導或辨識玩家真實身分。
- 模型改版必須更新模型版本。
- 每次訓練保存資料時間範圍及參數。
- 正式展示前比較不同語言與活躍程度族群的模型表現。

## 12. 開發順序

### P1：資料集與基準模型

- 建立玩家序列資料集。
- 實作 M0、M1、M2、M3。
- 建立時間切分與防止資料洩漏測試。
- 輸出 JSON／CSV 評估結果。

### P2：結果保存與儀表板

- 新增 `prediction_runs`。
- 保存模型設定與評估指標。
- 儀表板加入模型比較。
- 加入 CLI 訓練及匯出指令。

### P3：線上影子預測

- 新增 `move_predictions`。
- 每局開始時預測，但不影響遊戲。
- 玩家出拳後評估預測。
- 比較離線與線上表現。

### P4：電腦預測對戰

- 新增「挑戰預測 AI」模式。
- 保存模型選拳與玩家結果。
- 加入隨機對照組，確認模型是否真的提高勝率。

## 13. 第一個實作里程碑驗收

- 可從現有 SQLite 產生不含暱稱與 IP 的序列資料。
- 一局可正確轉換為兩筆玩家樣本。
- 特徵不包含當局或未來出拳。
- M0～M3 可用同一介面訓練與預測。
- 可用時間順序切分並輸出四項主要指標。
- 空資料與少量資料不會造成程式錯誤。
- 所有新增功能有自動化測試。
- 不影響既有雙人遊戲、資料收集與分析儀表板。
